<div id="error">
    <h1>403 Forbidden</h1>
    <p>
        <%=  
            case @exception
            when ApplicationController::IpAddressRejected
                "あなたのIPアドレスは （#{request.ip}）からは利用できません。"
            else
                "指定されたぺージを閲覧する権限がありません。"
            end
        %>
    </p>
</div>